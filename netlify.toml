[build]
  command = "npm run build"
  publish = "build"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[dev]
  command = "npm run start"
  targetPort = 3000
  port = 8888
  publish = "build"
  autoLaunch = false
